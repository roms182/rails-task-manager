<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>
<% @tasks.each do |task| %>
  <ul>
    <li> <%= task.name %> | <%= task.who %> |
      <%= link_to 'see', task %> #
      <%= link_to 'delete', task, :method => :delete, data: { confirm: "Are you sure?" } %> #
      <%= link_to 'edit', edit_task_path(task) %></li>
  </ul>
<% end %>

<p><%= link_to 'Add a task', task_path('new') %></p>
